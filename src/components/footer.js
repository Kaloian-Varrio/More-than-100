export function createFooter() {
  return `
    <footer class="border-top bg-white py-4 mt-auto">
      <div class="container text-center text-body-secondary small">
        <p class="mb-0">&copy; ${new Date().getFullYear()} More Than 100. Built for healthier everyday choices.</p>
      </div>
    </footer>`;
}
